kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: apache-claim
  namespace: dev
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: my-local-storage
  resources:
    requests:
      storage: 0.5Gi
